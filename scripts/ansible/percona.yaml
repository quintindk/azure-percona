- name: Percona
  hosts: localhost
  gather_facts: yes
  become: yes
  vars_files:
    - vars/env.yaml
  vars:
    update_apt_cache: yes
    ansible_python_interpreter: /usr/bin/python3
  roles:
    - docker
    - mount-data-disks
    - percona
    - mysql-users
    - pmm-server
    - pmm-client